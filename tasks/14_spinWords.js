/**
 * Задание. Напишите функцию, которая принимает строку из одного или нескольких слов и возвращает ту же строку, 
 * но с перевернутыми словами из пяти или более букв. 
 * Переданные строки будут состоять только из букв и пробелов. 
 * Пробелы одиночные и только если в строке более одного слова.
 * Примеры:
 *      spinWords( "Hey fellow warriors" ) => returns "Hey wollef sroirraw"
 *      spinWords( "This is a test") => returns "This is a test"
 *      spinWords( "This is another test" )=> returns "This is rehtona test"
 */

 function spinWords(text) {

 }

//  Тесты
console.assert(spinWords("Привет"), "тевирП");
console.assert(spinWords("Привет товарищи войны"), "тевирП ищиравот ынйов");
console.assert(spinWords("Это есть тест"), "Это есть тест");
console.assert(spinWords("Это другой тест"), "Это йогурд тест");